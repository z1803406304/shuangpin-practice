# 统一换行符，避免 Windows / macOS / Linux 之间来回改行尾
* text=auto eol=lf

# 二进制文件不要做文本转换
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary

# 生成产物在 diff 里折叠
src/data/*.ts linguist-generated=true
package-lock.json linguist-generated=true
